import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="Header">
            <a id="header__title">AppointMe</a>
        </div>
    );
}

export default Header;
